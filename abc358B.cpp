#include<bits/stdc++.h>
using namespace std;

int main(){
    int N,A;
    bool wait = false;
    cin >> N >> A;
    vector <int>t(N);
    for(int i = 0;i < N;i++){
        cin >> t.at(i);
    }
    for(int i = 0;i < N;i++){
        if(wait){
            
        }
    }
}