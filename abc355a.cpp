#include<bits/stdc++.h>
using namespace std;

int main(){
    int a,b;
    cin >> a >> b;
    int ans = 0;
    // if(!(a==b)) ans = 6-(a+b);
    if(!(a==b)) cout << 6-(a+b) << endl;
    else cout << -1 << endl;
}